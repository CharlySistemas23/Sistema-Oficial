#!/bin/bash
# Railway ya está en el directorio backend (Root Directory configurado)
# No necesitamos hacer cd backend
export LANG=C.UTF-8
export LC_ALL=C.UTF-8
export LANGUAGE=C.UTF-8
npm start
